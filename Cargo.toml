[package]
name = "svo-rs"
version = "0.1.0"
edition = "2018"

[dependencies]
micromath = { version = "2.0", optional = true }
nalgebra = { version = "0.27", default-features = false, features = [ "macros" ] }

[features]
default = [ "std" ]
std = []
no-std = [ "micromath" ]
